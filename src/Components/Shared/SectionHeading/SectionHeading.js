import React from 'react';

const SectionHeading = ({ children }) => {
 return (
  <div className="sectionFour-title sections-title text-center">
   <h2 className='title-text'>{children}</h2>
  </div>
 );
};

export default SectionHeading;